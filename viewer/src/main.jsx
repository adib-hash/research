import React from "react";
import ReactDOM from "react-dom/client";
import AILabRPG from "./ai-lab-rpg.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AILabRPG />
  </React.StrictMode>
);
